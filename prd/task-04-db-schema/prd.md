Design the database schema for the AI agent using prisma and SQLite

- store model providers
- store each agent, include model, prompt, mcp servers groups, and more
- store conversation history
- store chat meessages, include tool calls and more
- store mcp servers groups, maintain each mcp servers json config
- store file info, but keep file content in local file system

important:

- use json field to store complex data
